# Data

Raw experiment recordings are not committed to this repository.

Place local raw ROS bags under `bags/` when reproducing analysis runs:

```text
bags/
  thesis_final/
    *.bag
    *.bag.gz
```

The curated public outputs derived from those recordings are committed under [`../results/`](../results/).
